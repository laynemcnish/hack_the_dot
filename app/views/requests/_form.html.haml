= form_for @request, html: {class: 'form-horizontal' } do |f|
  - if @request.errors.any?
    #error_explanation
      %h2= "#{pluralize(@request.errors.count, "error")} prohibited this request from being saved:"
      %ul
        - @request.errors.full_messages.each do |msg|
          %li= msg

  .field
    = f.label "Text to Translate:"
    = f.text_area :body, class: 'form-control'
  .field
    = f.hidden_field :language, value: 'hp'
  .actions
    = f.submit 'Translate', class: 'btn btn-sm'
